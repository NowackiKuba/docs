---
openapi: "POST /api/customers"
---

Register a new end-user within the Billy platform.

<Note>
  **Stripe Synchronization:** Calling this endpoint automatically provisions a mapped Stripe Customer object under the hood.
</Note>

