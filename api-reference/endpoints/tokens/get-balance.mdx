---
openapi: "GET /api/tokens/balance/{customerId}"
---

Retrieve the real-time aggregated balance of a customer.

Returns the `available`, `frozen`, and `total` token amounts computed from the immutable ledger.

