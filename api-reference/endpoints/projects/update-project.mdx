---
openapi: "PATCH /api/projects/{id}"
---

Update mutable core project fields.

<Note>
  **Status Transitions:** Archiving a project via status update will immediately pause all billing logic and disable new subscription creations. 
</Note>

