---
openapi: "GET /api/alerts/project/{projectId}"
---

Poll structured billing events and notifications.

<Note>
  Use this endpoint to drive in-app notification centers (e.g., "Your card failed to charge") instead of building complex webhook listeners.
</Note>

