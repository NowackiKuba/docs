---
openapi: "POST /api/portal/sessions"
---

Generate a short-lived magic link to the Stripe Customer Portal.

<Note>
  Provide a `returnUrl` to ensure the customer is redirected seamlessly back into your application after managing their billing details.
</Note>

