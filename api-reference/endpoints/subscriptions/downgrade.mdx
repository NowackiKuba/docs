---
openapi: "PATCH /api/subscriptions/{id}/downgrade"
---

Downgrade a subscription to a lower tier.

<Note>
  By default, downgrades are scheduled at the end of the current billing cycle to prevent complex mid-cycle credit adjustments.
</Note>

