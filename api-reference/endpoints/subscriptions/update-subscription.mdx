---
openapi: "PATCH /api/subscriptions/{id}"
---

Modify general metadata or cancellation behavior for a subscription.

<Note>
  To change plans, use the explicit `/upgrade` or `/downgrade` endpoints instead to ensure correct proration logic is applied.
</Note>

